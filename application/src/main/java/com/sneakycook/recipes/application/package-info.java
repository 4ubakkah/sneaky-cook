/**
 * Spring-free use cases (create, update, delete, get, search) orchestrating the
 * domain through the repository port. One use-case execution equals one
 * transaction, demarcated by the infrastructure adapter.
 */
package com.sneakycook.recipes.application;
