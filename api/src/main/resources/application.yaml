spring:
  application:
    name: recipe-api
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/recipes}
    username: ${DB_USERNAME:recipes}
    password: ${DB_PASSWORD:recipes}
  jpa:
    # Flyway owns the schema; Hibernate only checks the mapping against it.
    hibernate:
      ddl-auto: validate
    open-in-view: false
