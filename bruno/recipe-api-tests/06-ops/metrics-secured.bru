meta {
  name: [REQ-17] Metrics require a token
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/actuator/metrics
  body: none
  auth: none
}

tests {
  test("401 without a token", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
