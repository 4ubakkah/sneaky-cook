meta {
  name: [REQ-18] Register user B
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "{{userB}}",
    "password": "{{password}}"
  }
}

tests {
  test("201 created", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
